<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/main_nav"
    app:startDestination="@id/fragmentTasks">
    <fragment
        android:id="@+id/fragmentTasks"
        android:name="com.test.todo.presentation.tasks.TasksFragment">
        <action
            android:id="@+id/action_task_to_new"
            app:destination="@id/fragmentNewTask" />
    </fragment>

    <fragment
        android:id="@+id/fragmentNewTask"
        android:name="com.test.todo.presentation.newTask.NewTaskFragment" />
</navigation>